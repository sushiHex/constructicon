# M7.1 implementation record

Status: PR A merged as #23 (`b5ff31f`); PR B implemented and locally verified,
awaiting independent review. Milestone closure awaits PR B's merge.

Implementation base: `8262d4fd614028ebb0b41b77ef124ff47afcc5dc`, the squash
merge of planning PR #22 on 2026-09-06. The owner explicitly approved and
merged the plan, accepting [ADR 0017](../../adr/0017-panel-membership-is-an-authored-map.md).
The approved [rev 4](../milestones/M7.1-panel-membership-rev4.md) stays byte-for-byte
unchanged; its draft status is historical. Current approval is recorded here
and in the planning index, not by rewriting the approved document.

## PR A: authored membership

The implementation uses private map entries retaining their connection index
and exact proposal or retained-definition coordinate. Ordinary nodes and Loop
boundaries call the same binding function. Each distinct selector supplies one
scalar producer; connection order governs the union, map-object key order is
canonicalized, and an explicit destination never silently disappears.

`panel()` emits those maps and requires at least two selector-representable
members. The exact request/result distinction and unique result-typed gather
remain local authoring checks. Admission proves the mapped result seats, not
an unstated request boundary or unrelated outputs.

One private version selector serves preflight and compilation. An exact lock
never consults stable. Counterfactual admission first validates the exact
baseline, then any requested overrides; successful overrides must retain the
source's complete contract hash at every affected exact scope. Baseline
invalidity is `REQUEST_INVALID`; override invalidity is
`COUNTERFACTUAL_LOCK_MISMATCH`. Both use the existing durable rejection law.

`SystemDescription` and its digest domain advance together to 2, publishing
scalar source cardinality separately from ordered mapped-many fan-in. Graph,
manifest, journal, and registry formats remain unchanged. The walker changes
nothing.

## Verification inventory

- `tests/runtime/test_explicit_membership.py`: ordered union and deduplication,
  scalar-source and destination validation, public JSON and direct runtime
  admission, Loop parity, exact nested/retained coordinates, canonical fault
  order, grammar-specific selector repair, and zero/multiple-source refusal.
- `tests/runtime/test_membership_compatibility.py`: complete canonical-manifest
  SHA-256 fingerprints from issue-to-summary, gathering, mapped-loop,
  architect-selection, and diamond, plus the post-review empty-node-selector
  fixture captured from the pre-M7.1 validator. All six match.
- `tests/sdk/test_panel_membership.py` and the existing combinator suite:
  selector representability, drift refusal, mapped/unmapped binding equivalence,
  literal Graph equality, pool exclusion, and explicit boundary limitations.
- `tests/api/test_membership_admission.py`: schema-2 publication/digest,
  lock-aware preflight, whole-boundary override rejection at repeated scopes,
  retained non-scalar baseline invalidity, and byte-stable refusal replay.
- `scripts/check_m71_mutations.py`: isolated code-object mutations; source files
  are never modified. Collection/setup errors are not counted as killed mutants.

Initial local gate at `8276581`: `uv run verify` passed Ruff, strict mypy, all
four import contracts, and 1,416 tests (60 more than the planning baseline).
All 15 initial isolated law mutants were killed by test failures. No collection
or setup failure is credited.

The historical non-scalar fixture was also compared against the validator
loaded from committed base `8262d4f`: its complete canonical manifest bytes
match that validator's output. It is genuine retained behavior, not an
exemption in current admission.

The pre-change repository map inventory consists of scalar selections in
architect-selection, mapped-loop, loop-failure, loop-closure, and loop-export
fixtures, plus the SDK flow map carrier. The source declarations and retained
composites exercised by those fixtures remain compatible; all five manifest
fingerprints and the full acceptance suite pass. External development databases
were not inventoried. Previously accepted explicit optional/many producers
must be adapted and re-admitted; their retained execution bytes are not edited.

All 31 planning manifest entries and all 52 relative planning links validate.
The approved rev-4 digest remains
`6d149303505df1adc78c208b40cac471605d750ad68be0700616899738ced248`.

## PR A review corrections

The owner relayed an independent exact-head review of `8276581` requesting two
changes. Both corrections use the existing framed map-fault path:

- Distinct maps into a `one` or `optional` destination retain exact coordinates.
  The first conflicting distinct selector owns the fault; evidence also names
  the first selector and its connection index. Repeated identical entries still
  coalesce before choosing that coordinate.
- Selectors missing a port segment regain the base validator's grammar refusal:
  `must be 'node.port' or '$input.port'`. Their repair asks for that segment,
  not a scalar adapter. Well-formed selectors resolving to no source retain
  the separate source-contract refusal.

One private table supplies the map-defect vocabulary, codes, and repairs to
both frame recognition and typed classification. Coordinates still come only
from the shared map-fault writer; no graph, manifest, or public fault code is
added.

Before the fixes, the strengthened focused suite produced 26 failures and 21
passes. Afterward, all 47 cases passed, including inline, Loop, retained, and
retained-inline ownership for both corrections. The isolated mutation inventory
now kills 18/18 mutants: the added checks remove the duplicate's coordinate
frame, point it at the wrong selector, and remove the grammar guard.

Post-correction `uv run verify` passed Ruff, strict mypy, all four import
contracts, and 1,437 tests (21 more than the reviewed head). The owner
independently confirmed those checks, all 18 mutants, and the base compatibility
goldens on `2313552`. Its confirming review was resolved before #23 merged;
the squash commit `b5ff31f` has the exact reviewed tree. The recorded
unknown-endpoint item stays in PR B.

The confirming review raised `.port` as a possible malformed selector. That
finding was refuted on both the reviewed head and base `8262d4f`: `NodeId`
permits the empty string, so a declared empty-id node is addressed by `.port`.
The complete admitted manifest bytes match the base. Without that node the
selector names no source, which is a source-contract refusal, not a grammar
error. General identifier syntax is explicitly unchanged by approved rev 4;
adding a non-empty-node guard would break that promise. The owner independently
reproduced both cases and reclassified the finding as consistent with ADR 0017.

A separate post-merge follow-up pins that distinction through public JSON
admission and a sixth base-manifest fingerprint. The mutation inventory also
tries adding the non-empty-node guard; the compatibility test must reject that
change. No production syntax or validation rule changes in this follow-up.

## PR B: connection endpoints

Implementation base: `b5ff31f362ded8d486e8473a9cb23c2cffba661b`, the exact
reviewed PR A tree squash-merged as #23. The empty-id compatibility follow-up
is a separate first commit; endpoint implementation follows it.

Each graph level checks its connection endpoints against that level's declared
node ids before reachability or node compilation. One invalid connection emits
one `unknown_connection_node` fault, with missing roles ordered `src`, then
`dst`; its source, destination, and connection index remain explicit evidence.
That level stops rather than compiling a graph with discarded edges. The
reachability helper consequently has no silent unknown-endpoint branch.

The existing coordinate writer is shared by maps and connections as
`_located_fault`. `_GraphLocation` still names either exact proposal bytes or
one retained definition's bytes. One `_LOCATED_FAULTS` table supplies frame
recognition and typed repairs. There is no extra preflight-only validator,
public fault code, Graph schema, or walker rule.

Endpoint identity is exact string membership, not selector grammar: empty and
dot-containing ids remain legal when declared, while `$input` is a selector
prefix, not an invented connection endpoint. Connection existence and map
resolution are independent obligations; a valid map cannot excuse an absent
endpoint. Neither rule is connector liveness.

Verification inventory:

- `tests/runtime/test_connection_endpoints.py` covers source, destination, and
  both missing roles, with and without maps, at root, inline, Loop, retained,
  retained-inline, and retained-Loop ownership. Public JSON and direct runtime
  admission agree on the exact frame. Tests also pin early refusal, bounded
  deterministic diagnostics, punctuation-safe scopes, and exact id membership.
- `tests/api/test_endpoint_admission.py` exercises historical baseline refusal
  with and without overrides at all six ownership shapes. Exact retries retain
  response bytes, create no new run or launch, and leave the source untouched.
  Reproduce remains byte-identical to the retained manifest. New-start refusal
  survives its two actual response-loss seams and requires a fresh key after
  repair. A separate probe proves there is no domain-mutation seam or orphan
  run, manifest, or effect to recover from an admission refusal.
- The six [historical manifests](../../../tests/fixtures/m71/ghost-endpoint-manifests.json)
  were captured from the committed PR A validator before the endpoint change.
  Tests read those facts; they never disable current validation to manufacture
  their own historical evidence. Each fixture retains its manifest hash and
  names the exact writer commit.
- [The provenance checker](../../../scripts/check_m71_fixture_provenance.py)
  independently reproduces all six complete manifests from `b5ff31f` and the
  empty-id golden from `8262d4f`. It needs those local commits and is not a CI
  dependency. Run `uv run python scripts/check_m71_fixture_provenance.py`.
- The mutation inventory kills 26/26 mutants, including endpoint enforcement,
  early stopping, each endpoint role, role ordering, exact connection
  coordinates, and historical baseline classification. The original PR A
  mutations remain live after the shared coordinate writer extraction.

The initial endpoint suite failed on PR A in 39 cases and passed the three
legal-identifier controls. The focused combined admission/compatibility suite
then passed 111 cases; four additional absent-identifier cases join the full
gate. Final `uv run verify` passed Ruff, strict mypy, all four import contracts,
and 1,500 tests (63 above merged PR A). Both proof scripts passed: 26/26 mutants
killed, all six historical manifests reproduced, and the empty-node base
golden reproduced. Independent exact-head review remains pending; local
inspection is not recorded as an independent review.

## Remaining scope

PR B must pass CI and independent exact-head review before merge. This
record does not describe the merged map surface as total until both slices
merge. Existing unmapped history is not
retrofitted. Reproduce retains that history's manifest; counterfactual applies
current admission law and the stronger complete override-boundary check.

Current architecture/contribution documentation and the M7 closure record are
updated only at the milestone closure prescribed by rev 4.
